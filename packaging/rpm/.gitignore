# rpmbuild working tree — generated by `make` (Makefile copies the canonical
# heimdall-server.spec into rpmbuild/SPECS/ at build time).
rpmbuild/

# Downloaded/generated build inputs and outputs
*.rpm
*.tar.gz
*.src.rpm
