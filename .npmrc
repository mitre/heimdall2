# pnpm configuration
strict-peer-dependencies=false
auto-install-peers=true
link-workspace-packages=true

# Symlink node_modules (faster, less disk space)
node-linker=hoisted

# For compatibility if needed (try without first)
# shamefully-hoist=true
