{
  "name": "@heimdall/password-hash-vectors",
  "version": "2.13.0",
  "license": "Apache-2.0",
  "description": "Password-hash format contract for Heimdall: malformed-hash corpus and (follow-on) known-good vectors shared between heimdall2 and heimdall-cli",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/mitre/heimdall2",
    "directory": "libs/password-hash-vectors"
  },
  "main": "src/index.ts",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "gen:vectors": "tsx scripts/write-vectors.ts",
    "lint": "eslint --fix",
    "lint:ci": "eslint --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.0",
    "tsx": "^4.20.0",
    "vitest": "^4.0.18"
  }
}
