// import * as crypto from 'crypto';

// export default registerAs('jwt', () => ({
//   jwtSecret: process.env.JWT_SECRET || generateDefault()
// }))

// function generateDefault(): string {
//   return crypto.randomBytes(64).toString('hex');
// }
