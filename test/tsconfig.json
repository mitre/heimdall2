{
  "extends": "../tsconfig.json",

  "compilerOptions": {
    "types": ["cypress", "node"],
    "lib": ["esnext", "dom"],
    "allowJs": true,
    "noEmit": true,

    "ignoreDeprecations": "6.0"
  },

  "include": [
    "../cypress.config.ts",
    "**/*.ts",
    "services/*.mjs"
  ]
}
